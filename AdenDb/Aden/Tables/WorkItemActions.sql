CREATE TABLE [Aden].[WorkItemActions] (
    [WorkItemActionId] INT          NOT NULL,
    [Name]             VARCHAR (50) NULL,
    CONSTRAINT [PK_WorkItemAction] PRIMARY KEY CLUSTERED ([WorkItemActionId] ASC)
);

